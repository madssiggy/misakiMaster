using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScoreDisp : MonoBehaviour
{
    /*
    private GameObject medal;
    const string gold      = "Textures/gold";
    const string silver    = "Textures/silver";
    const string bronze    = "Textures/bronze";
    private Texture Tex_gold;
    private Texture Tex_silver;
    private Texture Tex_bronze;
    // Start is called before the first frame update
    void Start()
    {
        medal = GameObject.Find("medal");
        SpriteRenderer spriteObj = medal.GetComponent<SpriteRenderer>();

        Tex_gold = Resources.Load<Texture>(gold);
        Tex_silver = Resources.Load<Texture>(silver);
        Tex_bronze = Resources.Load<Texture>(bronze);

        spriteObj.sprite = Tex_silver;
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    */
}
